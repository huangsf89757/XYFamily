# 02-缓存设计方案

> P5 支撑域之二。定义 Redis 在全系统的缓存使用全景：验证码、Token 黑名单、权限缓存、登录限流、热点数据。承接 [P2 RBAC §六（权限缓存）](../../03-核心域/02-RBAC权限引擎方案.md) 与 [P2 JWT §五（黑名单）](../../03-核心域/03-JWT鉴权链与Token方案.md)，满足 NFR-PERF-008（命中率 ≥95%）、NFR-PERF-001（95% < 100ms）。

---

## 文档信息

| 项目 | 内容 |
|------|------|
| 文档密级 | 内部 |
| 文档版本 | V1.0.0 |
| 编写人 | ClaudeCode |
| 审核人 | - |
| 生效时间 | 2026-07-15 |
| 关联标签 | 技术方案、缓存、Redis、性能 |
| 关联目录 | 03-架构与方案设计/06-支撑域 |

## 变更记录

| 版本 | 日期 | 变更内容 | 变更人 |
|------|------|----------|--------|
| V1.0.0 | 2026-07-15 | 汇总 Redis 缓存全景与键规范 | ClaudeCode |

---

## 一、缓存键全景

| 用途 | Key | 类型/值 | TTL | 依据 |
|------|-----|---------|-----|------|
| 验证码 | `vc:{target}:{type}` | String/Hash `{code, attempt_count, expires_at}` | 5min | ADR-012、NFR-SEC-003 |
| Token 黑名单 | `bl:{jti}` | String（空值） | = Access 剩余有效期 | ADR-003 |
| 权限缓存 | `perm:account:{account_id}:org:{org_id}` | Set（权限点集合） | 24h | ADR-007、P2 RBAC §六 |
| 登录限流(IP) | `rl:ip:{ip}` | String（计数） | 300s 窗口 | NFR-SEC-002 |
| 登录限流(账号) | `rl:acct:{account_id}` | String（计数） | 300s 窗口 | NFR-SEC-002 |
| 热点组织信息 | `org:info:{org_id}` | Hash（name/desc/owner） | 300s | NFR-PERF-008 |
| 审计 Stream | `audit_stream` | Stream | 持久 | ADR-011 |

---

## 二、权限缓存（核心）

- **读取路径**（[P4 中间件 PermissionChecker](../05-链路实现/01-中间件链专项方案.md)）：先查 `perm:account:{id}:org:{org_id}`；未命中查 `role_permissions` 预填充展开并回填。
- **失效时机**（主动删除 key）：
  - 角色/成员关系变更（分配/降级/移除/邀请接受）
  - 权限矩阵重新预填充（[P2 RBAC §五](../../03-核心域/02-RBAC权限引擎方案.md)）
- **降级**：Redis 不可用时回退 DB 查询，不阻断业务（仅性能下降）。
- **命中率目标 ≥95%**（NFR-PERF-008）。

---

## 三、Token 黑名单

- 写入：登出（当前 Access jti）、改密（当前 Access jti）、重置（该账号全部会话相关 jti）。
- TTL 设为 Access 剩余有效期，避免永久存储、自然过期。
- 每次请求在 JWTValidator 之后查一次（<1ms）。

---

## 四、登录限流

- 双维度：`rl:ip:{ip}`（5min 5 次 → 锁 15min）+ `rl:acct:{account_id}` 叠加。
- 命中返回 `429`（104290/104291）。
- 使用原子递增 + `EXPIRE` 窗口。

---

## 五、热点数据

- 组织信息、角色权限等读多写少数据缓存（NFR-PERF-008）。
- 写操作（更新组织/角色）后失效对应缓存。

---

## 六、高可用与运维

- Redis Sentinel 保证高可用（[P1 部署架构](../../02-基座/01-整体架构设计.md)）。
- 缓存雪崩防护：TTL 加随机抖动；穿透防护：空值短缓存；击穿防护：单 key 重建加分布式锁。
- 监控：命中率、内存、连接数（[P6 可观测性](../../07-横切专项/03-可观测性方案.md)）。

---

## 七、关联文档

- [RBAC 权限引擎方案](../../03-核心域/02-RBAC权限引擎方案.md) §六
- [JWT 鉴权链](../../03-核心域/03-JWT鉴权链与Token方案.md) §五
- [中间件链专项方案](../05-链路实现/01-中间件链专项方案.md)
- [P6 可观测性方案](../../07-横切专项/03-可观测性方案.md)
