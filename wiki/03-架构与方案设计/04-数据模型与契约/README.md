# 04-数据模型与契约（P3）

> P3 阶段：把 P2 核心域的领域模型落库为数据库结构，并对外暴露为接口契约。强依赖 P2。

## 文档清单（规划）

| 文档 | 状态 | 说明 |
|------|------|------|
| [01-数据库设计/](./01-数据库设计/README.md) | ✅ 已编写 | 全量 PG DDL、ER 图、枚举、索引、预初始化数据；按域拆 5 份 |
| [02-接口设计/](./02-接口设计/README.md) | ✅ 已编写 | 各模块 API 契约（路径/请求响应/错误码/鉴权范围）；按 PRD 模块拆 8 份 |

## 依赖

- 前置：03-核心域（租户隔离决定 `org_id` 贯穿所有表；RBAC 决定角色/权限表；JWT 决定 `sessions`/`token_blacklist` 形态；账号生命周期决定 `status` 状态机与软删）。
- 产出：为 P4 链路实现（中间件链、时序图、审计）提供数据结构与契约依据。

## 编写顺序

01-数据库设计 → 02-接口设计（DB 先于接口：接口数据形状来自表结构；接口再反哺 DB 校验）。
