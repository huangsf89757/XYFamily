# 前端通用代码规范

> XYFamily **跨平台前端统一约定**，适用于安卓、iOS、H5、微信小程序、Web 五个平台。各端具体选型请参考对应端的规范文档。

---

## 文档信息

| 项目 | 内容 |
|------|------|
| 文档密级 | 内部 |
| 文档版本 | V1.1.0 |
| 编写人 | ClaudeCode |
| 审核人 | - |
| 生效时间 | 2026-07-12 |
| 废弃时间 | - |
| 关联标签 | 核心文档、规范标准 |
| 关联目录 | 01-项目总览/标准与规范/03-分端代码规范 |

## 变更记录

| 版本 | 日期 | 变更内容 | 变更人 |
|------|------|----------|--------|
| V1.0.0 | 2026-07-12 | 初始创建 | ClaudeCode |
| V1.1.0 | 2026-07-16 | 移除通用规范，仅保留项目特有约定 | ClaudeCode |

---

## 一、各平台概览

| 平台 | 语言/框架 | 定位 | 详细规范 |
|------|-----------|------|----------|
| 安卓 | Kotlin + Jetpack Compose | Android 原生 App | [安卓Kotlin代码规范](./04-安卓Kotlin代码规范.md) |
| iOS | Swift + SwiftUI | iOS 原生 App | [苹果Swift代码规范](./05-苹果Swift代码规范.md) |
| H5 | React + TypeScript | 移动 H5 页面（浏览器） | [H5代码规范](./03-H5代码规范.md) |
| 微信小程序 | 原生 / uni-app | 微信生态小程序 | [微信小程序代码规范](./06-微信小程序代码规范.md) |
| Web | React + TypeScript | 管理后台（PC 浏览器） | [Web代码规范](./02-Web代码规范.md) |

**原则**：跨平台 UI 组件使用共享设计语言（Design System），API 调用层统一封装，平台特有功能各端自实现。

---

## 二、API 调用统一规范

### 2.1 请求格式

所有平台使用统一的 HTTP Client 封装，统一处理：

- 自动携带 `Authorization` Header
- 自动携带 `X-Organization-ID` / `X-Team-ID` / `X-Group-ID` Header
- Token 过期自动刷新（Access Token 30min → 使用 Refresh Token 刷新）
- 统一错误处理（HTTP 401 → 跳转登录）

```typescript
// 各平台统一响应格式（各端按语言实现等价逻辑）
interface APIResponse<T> {
  code: number;       // 0=成功，10xxx-80xxx=业务错误
  message: string;    // 错误摘要
  detail?: string;    // 详细错误
  data?: T;
}
```

### 2.2 错误码处理（跨平台统一）

| HTTP Status | 业务 Code 段 | 处理策略 |
|-------------|------------|---------|
| 401 | 101001-101009 | 统一跳转登录 |
| 401 | 101004-101006 | Refresh Token 失效 → 强制重新登录 |
| 429 | 104290-114291 | 展示限流提示，禁用操作 |
| 403 | 603001-603002 | 展示"无权限"提示 |
| 500 | 805000-805003 | 展示"服务异常，请稍后重试" |

**各平台错误提示统一文案**：
- 401 Token 过期：`"登录已过期，请重新登录"`
- 429 限流：`"操作过于频繁，请稍后再试"`
- 403 无权限：`"当前账号无权限执行此操作"`

---

## 三、多租户上下文传递

### 3.1 Header 传递规则

| Header | 必填时机 | 说明 |
|--------|---------|------|
| `X-Organization-ID` | 操作组织资源时 | 从当前选中的组织上下文获取 |
| `X-Team-ID` | 操作团队资源时 | 从当前选中的团队上下文获取 |
| `X-Group-ID` | 操作小组资源时 | 从当前选中的小组上下文获取 |

### 3.2 多组织切换

用户可能同时属于多个组织，前端需支持组织切换：
- 登录后展示用户所属组织列表
- 切换组织时更新全局上下文（`currentOrgId`）
- 切换组织后 Token Claims 中的 `org_ids` 不变，但 Header 中的 `X-Organization-ID` 更新

---

## 四、本地存储规范

### 4.1 存储内容

| 存储项 | 存储方式 | 说明 |
|--------|---------|------|
| Access Token | 内存（不持久化） | 防持久化泄露，页面刷新后需重新获取 |
| Refresh Token | 平台安全存储 | Android Keystore / iOS Keychain / 加密 localStorage |
| 用户信息 | 本地持久化（加密） | 减少不必要的接口请求 |
| 当前组织 ID | 本地持久化 | 保持用户组织上下文 |

### 4.2 安全要求

- Access Token 不持久化到本地存储
- Refresh Token 使用平台安全存储，不可明文存储
- 登出时清除所有本地 Token

---

## 五、Token 自动刷新策略

```
请求 → 401 (Access Token 过期)
  → 使用 Refresh Token 请求 /auth/refresh
  → 刷新成功：用新 Access Token 重试原请求
  → 刷新失败（Refresh Token 已撤销/过期）：清除本地 Token → 跳转登录
```

**并发处理**：多个请求同时过期时，只发起一次刷新，其他请求等待刷新结果。

---

## 六、关联文档

- [后端 Go 代码规范](./01-后端Go代码规范.md)
- [安卓(Kotlin)代码规范](./04-安卓Kotlin代码规范.md)
- [iOS(Swift)代码规范](./05-苹果Swift代码规范.md)
- [H5代码规范](./03-H5代码规范.md)
- [微信小程序代码规范](./06-微信小程序代码规范.md)
- [Web代码规范](./02-Web代码规范.md)
- [接口总览](../../../04-接口文档/接口文档.md)

## 关联文档


> 以下为知识图谱自动推荐的交叉引用，建议人工审阅确认后保留。

- [架构评审记录](../../../03-架构与方案设计/审核记录/架构评审记录.md) — 共享术语：token、多租户、安全（置信度 0.75）
- [04-Token管理](../../../02-需求与产品设计/01-产品PRD/01-多租户底座/01-用户认证模块/04-Token管理.md) — 共享术语：token、多租户、安全（置信度 0.75）
- [02-登录认证](../../../02-需求与产品设计/01-产品PRD/01-多租户底座/01-用户认证模块/02-登录认证.md) — 共享术语：token、多租户、安全（置信度 0.75）
- [用户认证模块](../../../02-需求与产品设计/01-产品PRD/01-多租户底座/01-用户认证模块/用户认证模块.md) — 共享术语：token、多租户、安全（置信度 0.75）
