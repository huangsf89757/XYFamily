# 微信小程序代码规范

> XYFamily 微信小程序（原生框架 / uni-app）**项目特定约定与选型**。通用微信小程序开发规范请直接参考官方文档，本文不再重复。

---

## 文档信息

| 项目 | 内容 |
|------|------|
| 文档密级 | 内部 |
| 文档版本 | V1.1.0 |
| 编写人 | ClaudeCode |
| 审核人 | - |
| 生效时间 | 2026-07-12 |
| 废弃时间 | - |
| 关联标签 | 核心文档、规范标准 |
| 关联目录 | 01-项目总览/标准与规范/03-分端代码规范 |

## 变更记录


| 版本 | 日期 | 变更内容 | 变更人 |
|------|------|----------|--------|
| V1.1.0 | 2026-07-19 | 文档新编 | ClaudeCode |

---

## 参考规范（官方标准）

- **[微信小程序开发指南](https://developers.weixin.qq.com/miniprogram/dev/framework/)** — 微信官方框架文档
- **[微信小程序设计指南](https://developers.weixin.qq.com/miniprogram/design/)** — 微信官方设计规范
- **[uni-app 官方文档](https://uniapp.dcloud.net.cn/)** — uni-app 框架文档

---

## 一、技术栈选型

| 类别 | 选型 |
|------|------|
| 框架 | 微信小程序原生 / uni-app（跨端复用） |
| 语言 | TypeScript |
| 网络 | wx.request / uni.request |
| 存储 | wx.setStorageSync（Refresh Token 加密） |
| 适配 | rpx（750rpx = 屏幕宽度） |
| 构建 | Vite（uni-app） / weapp-compiler |
| 分包 | 主包 + 按模块分包（认证 / 组织 / 团队 / 小组） |

---

## 二、项目目录结构

```
apps/miniprogram/
├── src/
│   ├── pages/
│   │   ├── login/                # 登录页
│   │   ├── register/             # 注册页
│   │   ├── profile/              # 个人页面
│   │   ├── org/                  # 组织页面
│   │   └── team/                 # 团队页面
│   ├── components/               # 公共组件
│   ├── utils/
│   │   ├── request.ts            # wx.request 封装
│   │   ├── token.ts              # Token 管理
│   │   └── permission.ts         # 权限校验
│   ├── store/
│   │   └── index.ts              # Vuex / Pinia 状态管理
│   ├── static/                   # 静态资源
│   └── app.vue                   # 入口
```

---

## 三、API 调用（项目 Header 约定）

```typescript
interface RequestOptions {
  url: string;
  method?: 'GET' | 'POST' | 'PUT' | 'DELETE';
  data?: any;
  skipAuth?: boolean;
}

function request<T>(options: RequestOptions): Promise<T> {
  return new Promise((resolve, reject) => {
    const token = getToken();
    const header: Record<string, string> = {
      'Content-Type': 'application/json',
    };

    if (token && !options.skipAuth) {
      header['Authorization'] = `Bearer ${token}`;
    }

    const orgId = getCurrentOrgId();
    if (orgId) {
      header['X-Organization-ID'] = orgId;
    }

    wx.request({
      url: `${API_BASE_URL}${options.url}`,
      method: options.method || 'GET',
      data: options.data,
      header,
      success: (res) => {
        const { code, data } = res.data;
        if (code === 0) {
          resolve(data as T);
        } else if (code >= 101001 && code <= 101009) {
          handleTokenExpired().then(() => resolve(request<T>(options)))
            .catch(() => reject({ code, message: res.data.message }));
        } else {
          reject({ code, message: res.data.message, detail: res.data.detail });
        }
      },
      fail: (err) => reject({ code: -1, message: '网络连接异常', ...err }),
    });
  });
}
```

---

## 四、Token 管理（项目 Key）

```typescript
const TOKEN_KEY = 'xyfamily_access_token';
const REFRESH_TOKEN_KEY = 'xyfamily_refresh_token';

function getToken(): string | null {
  return wx.getStorageSync(TOKEN_KEY) || null;
}

function setToken(token: string) {
  wx.setStorageSync(TOKEN_KEY, token);
}

function clearTokens() {
  wx.removeStorageSync(TOKEN_KEY);
  wx.removeStorageSync(REFRESH_TOKEN_KEY);
}
```

---

## 五、分包策略

| 包名 | 页面 | 限制 |
|------|------|------|
| 主包 | 登录、注册、首页 | < 2MB |
| 认证包 | 密码重置、绑定手机 | < 1MB |
| 组织包 | 组织管理、成员邀请 | < 1MB |
| 团队包 | 团队管理、小组管理 | < 1MB |

---

## 六、错误处理（项目错误码映射）

```typescript
function getErrorMessage(code: number): string {
  const map: Record<number, string> = {
    101001: '登录已过期，请重新登录',
    101002: '登录已过期，请重新登录',
    101003: '登录已过期，请重新登录',
    104290: '操作过于频繁，请稍后再试',
    603001: '当前账号无权限执行此操作',
    603002: '当前账号无权限执行此操作',
    805000: '服务异常，请稍后重试',
  };
  return map[code] || '请求失败，请重试';
}
```

---

## 七、关联文档

- [前端通用代码规范](./07-前端通用代码规范.md)
- [接口文档](../../../04-接口文档/接口文档.md)
