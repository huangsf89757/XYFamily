# 2026-07-16 工作日志

## 侧边栏折叠按钮重做（用户反馈）

用户要求：参考截图，把侧边栏折叠/展开按钮做成「贴在侧边栏右边缘分割线上的胶囊形按钮」，并**删除所有其他触发点**，只保留这一个。

调度 prototype-builder 修改 `wiki/02-需求与产品设计/02-原型与UI设计/01-网页端/index.html`（现 2726 行）：
- **删除旧触发点**：品牌区 `.sidebar-logo` 上的 `data-action="toggle-sidebar"`（变纯展示）；品牌区内 `.sidebar-collapse-btn` 整行删除。
- **新增唯一触发点**：`.sidebar-toggle-pill` 按钮，作为 sidebar 的兄弟元素插入 shell，定位 `position: fixed; top: 50%; left: var(--sidebar-w)`，跨在分割线上（宽 20px、高 48px、仅右圆角 8px、border-left:none）。
- **收起态**：`left` 变 `var(--sidebar-w-collapsed)`(64px) 跟随左移、箭头旋转 180°（◀变▶）；`transition: left` 平滑。
- **移动端**：≤820px 抽屉态 `display:none`，手柄隐藏（用汉堡按钮）。
- 边框 token 用 `var(--border)`（比 spec 给的 `--border-color` 更正确，dark 模式也对）。

### 验证
- grep 确认 `data-action="toggle-sidebar"` 的 DOM 元素全场仅 1 处（L1435）；`sidebar-collapse-btn` 残留 0；logo 无 data-action；`node --check` 通过；无新增硬编码 hex。

### 可复用经验
- 后台侧边栏折叠"把手"按钮标准做法：`position: fixed` 钉在视口分割线中部（不随页面滚动移动），比 `absolute` 相对容器更贴合"把手"体感。
- 用户偏好：侧边栏折叠触发点要单一、且明显在分割线上，不要藏在品牌区/logo 上。

## 顶部栏左侧按钮按页面层级智能切换（用户反馈）

用户要求：一级页面（侧边栏直连页面）顶部栏左侧**不显示按钮**；二级页面（子页）显示为**返回按钮**（← 返回，跳父级）；移动端仍用汉堡菜单。

调度 prototype-builder 修改 `index.html`（现 2792 行）：
- 新增 `getRouteLevel(route)` 工具函数：一级白名单 + 二级动态路由正则映射回父级（如 `#/org/members`→`#/org`、`#/team/:id/members`→`#/team/:id`、未知路由兜底 level:1）。
- 重写 `renderTopbar`：一级页 `leftBtn=''` 不渲染；二级页渲染 `.topbar-back-btn`(chevronLeft + "返回"，`data-action="go-back"`)。
- `handleAction` 新增 `go-back` case：读 `data-back-route` 跳父级。
- 顺带修复隐藏 bug：原 `.topbar-hamburger{display:none}` 特异性(0,1,0)低于后来定义的 `.icon-btn{display:inline-flex}`(0,1,0 但源码在后)，桌面汉堡实际一直显示；改为 `.icon-btn.topbar-hamburger{display:none}`(0,2,0) 解决。移动端 `@media` 内仍 `!important` 显示汉堡、隐藏返回按钮。
- `state.activeRoute` 在 renderRoute 中赋值供判断。

### 验证
- grep 确认 getRouteLevel/renderTopbar/go-back/activeRoute 均在；桌面隐藏汉堡规则特异性正确；`node --check` 通过；无新增硬编码 hex。

### 可复用经验
- 后台顶部栏按钮：一级页不显示、二级页显示"返回"（跳父级），是常见层级导航模式；CSS 桌/移切换用"桌面默认隐藏 + 移动 media `!important` 显示"模式，注意特异性要避免被通用 `.icon-btn` 规则覆盖。

## 三端原型交付（桌面端 / 移动端 / 小程序）

依据 PRD + 网页端参考 + 设计规范，补齐 02-桌面端、03-移动端、04-小程序 三端高保真交互原型（此前 01-网页端已完成）。

交付形态（与网页端一致）：各端 `index.html`（自包含单文件 SPA，内联 CSS/JS、零框架、hash 路由、Lucide 内联 SVG）+ `EXPORT-README.md` + `XYFamily-xx原型.zip`（10KB / 7.8KB / 7.2KB）。

- **桌面端**：原生窗口外壳（macOS 交通灯 / Windows 标题栏可切换）、可折叠侧栏、`Cmd/Ctrl+K` 命令面板、右键菜单、离线/更新横幅、7 视图（Dashboard/Org/Team/Group/Permissions/Audit/SuperAdmin）。
- **移动端**：iPhone 390×844，底部 4 Tab、Inset Grouped、安全区、6 视图、Action Sheet、深色开关。
- **小程序**：iPhone 375px（rpx 1px=2rpx），tabBar 4、微信授权登录（微信绿 `#07C160`）、扫码加入 / 邀请分享、9 视图。

四端统一消费《00-全局设计规范》语义令牌（Light/Dark 双主题），单一主色 `#007AFF`，角色标签色板一致。

### 父索引登记
更新 `02-原型与UI设计/原型与UI设计.md` → V1.0.2：把「效果图」占位（`⬜ 待创建`）改为「各端原型交付索引」（`✅ 已填充`），新增四端交付物链接表 + 覆盖范围摘要。

### 验证
- 三端 index.html 均以 `</html>` 正常收尾（42KB / 33KB / 31KB）；桌面 `location.hash` ×2，移动 `go()` ×16（6 视图），小程序 `go()` ×23（8 视图，含 `/scan` `/invite`）—— 路由机制完整。
- ZIP 打包 exit 0。

### 已知边界（演示性质）
登录、离线、推送、微信生态（wx.login/scanCode/shareAppMessage）均为前端交互示意，无真实后端；管理类重操作按规范收敛至 Web/桌面端。详见各端 EXPORT-README「已知简化」。
